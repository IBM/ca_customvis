// Declare the 'highcharts' module. This will generate a require dependency
// that is mapped in config.js to the CDN location of Highcharts.
declare module "highcharts";

/**
 * Can be used to provide exporting capabilities (dropdown menu in UI).
 * When used put the following statements in Main.ts:
 * import Exporting from "highcharts/modules/exporting";
 * Exporting;
 */
// declare module "highcharts/modules/exporting";
